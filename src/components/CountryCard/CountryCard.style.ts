import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    text: {
        fontSize: 36,
        textAlign: "center",
        fontWeight: "bold",
        color: "white"
    }
})

export default styles